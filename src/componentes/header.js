import "./header.css";


function Header(){
    return(
        <header className="header">
            <img src="/img/header.png" alt="Org"></img>
        </header>
    );
}

export default Header;
// la linea mas importante para que los componentes funciones de manera correcta